obs: na rota post precisamos utilizar o arquivo principal o use(express.urlencod({extended:true})) para liberar o acesso 
e rota post é um local onde o usuario não tem acesso 
para utilizar o post so com o form e precisamos criar um rota get para que funcione o get 